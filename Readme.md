# This is the Python Django official tutorial

## Create a virtual env
`python3 -m venv venv`

## Install dependencies
`pip install -r requirements.txt`


## Commands
`source bin/activate`

`python manage.py runserver`


`python manage.py makemigrations`

`python manage.py migrate`